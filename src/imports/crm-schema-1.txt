public.crm_activities
id — text — NOT NULL — default: none
type — USER-DEFINED — NULL — default: none
label — text — NULL — default: none
date — text — NULL — default: none
group — text — NULL — default: none
entity_type — text — NULL — default: none
entity_id — text — NULL — default: none
created_at — timestamp with time zone — NULL — default: now()
subject — text — NULL — default: none
description — text — NULL — default: none
status — text — NULL — default: none
priority — text — NULL — default: none
due_date — timestamp with time zone — NULL — default: none
start_date — timestamp with time zone — NULL — default: none
end_date — timestamp with time zone — NULL — default: none
completed_at — timestamp with time zone — NULL — default: none
assigned_to — text — NULL — default: none
related_to_type — text — NULL — default: none
related_to_id — text — NULL — default: none
related_to_name — text — NULL — default: none
contact_id — text — NULL — default: none
contact_name — text — NULL — default: none
location — text — NULL — default: none
all_day — boolean — NOT NULL — default: false
is_private — boolean — NOT NULL — default: false
tags — text — NULL — default: none
owner — text — NULL — default: none
body — text — NULL — default: none
updated_at — timestamp with time zone — NULL — default: now()
created_by — text — NULL — default: none
updated_by — text — NULL — default: none
meet_link — text — NULL — default: none
google_event_id — text — NULL — default: none
timezone — text — NULL — default: 'America/Sao_Paulo'::text
recurrence — text — NULL — default: none
is_recurring — boolean — NOT NULL — default: false
recurrence_interval — integer — NULL — default: none
attendees — jsonb — NULL — default: '[]'::jsonb
reminder — text — NULL — default: none
busy_status — text — NULL — default: 'ocupado'::text
visibility — text — NULL — default: 'padrao'::text
calendar_name — text — NULL — default: 'Zenite CRM'::text
call_duration — integer — NULL — default: none
call_type — text — NULL — default: none
call_result — text — NULL — default: none
call_direction — text — NULL — default: none
phone — text — NULL — default: none
channel — text — NULL — default: none
recipient — text — NULL — default: none
recipient_phone — text — NULL — default: none
sent_at — timestamp with time zone — NULL — default: none
read_at — timestamp with time zone — NULL — default: none
note_visibility — text — NULL — default: none
shared_with — jsonb — NULL — default: '[]'::jsonb
version — integer — NULL — default: 1
created_by — text — NULL — default: none (nota: já listada acima)
(Observação: algumas colunas podem repetir no output original; contei conforme retorno do information_schema.)
public.crm_call_records
id — text — NOT NULL — default: none
phone — text — NULL — default: none
date — text — NULL — default: none
avatar_url — text — NULL — default: none
entity_type — text — NULL — default: none
entity_id — text — NULL — default: none
created_at — timestamp with time zone — NULL — default: now()
activity_id — text — NULL — default: none
direction — text — NULL — default: none
title — text — NULL — default: none
duration — text — NULL — default: none
notes — text — NULL — default: none
updated_at — timestamp with time zone — NULL — default: now()
public.crm_field_history
id — text — NOT NULL — default: none
entity_type — text — NULL — default: none
entity_id — text — NULL — default: none
field_name — text — NULL — default: none
old_value — text — NULL — default: none
new_value — text — NULL — default: none
changed_by — text — NULL — default: 'Sistema'::text
changed_at — timestamp with time zone — NULL — default: now()
change_source — USER-DEFINED — NULL — default: ''ui'::crm_field_change_source
public.price_clients
id — uuid — NOT NULL — default: gen_random_uuid()
name — text — NOT NULL — default: none
email — text — NULL — default: none
company — text — NULL — default: none
phone — text — NULL — default: none
created_at — timestamp with time zone — NULL — default: now()
public.price_proposal_services
id — uuid — NOT NULL — default: gen_random_uuid()
proposal_id — text — NOT NULL — default: none
service_id — text — NULL — default: none
complexity — USER-DEFINED — NULL — default: 'basico'::service_complexity
recurrence — USER-DEFINED — NULL — default: 'mensal'::service_recurrence
seniority — USER-DEFINED — NULL — default: 'pleno'::service_seniority
allocation — USER-DEFINED — NULL — default: 'compartilhado'::service_allocation
include_impl — boolean — NULL — default: true
quantity — integer — NULL — default: 1
computed_monthly — numeric — NULL — default: 0
computed_impl — numeric — NULL — default: 0
computed_hours — integer — NULL — default: 0
public.price_proposals
id — text — NOT NULL — default: none
client_name — text — NOT NULL — default: none
status — USER-DEFINED — NULL — default: 'rascunho'::price_proposal_status
notes — text — NULL — default: none
global_discount — numeric — NULL — default: 0
combo_discount_percent — numeric — NULL — default: 0
combo_label — text — NULL — default: none
total_monthly — numeric — NULL — default: 0
total_impl — numeric — NULL — default: 0
grand_total — numeric — NULL — default: 0
total_hours — integer — NULL — default: 0
created_at — timestamp with time zone — NULL — default: now()
updated_at — timestamp with time zone — NULL — default: now()
tag — text — NULL — default: none
account — text — NULL — default: none
opportunity — text — NULL — default: none
contact — text — NULL — default: none
public.price_services
id — text — NOT NULL — default: none
name — text — NULL — default: none
service_group — text — NULL — default: none
description — text — NULL — default: none
base_price — numeric — NULL — default: 0
impl_price — numeric — NULL — default: 0
hours_estimate — numeric — NULL — default: none
is_ads — boolean — NULL — default: false
complexity_basico — numeric — NULL — default: 1
complexity_intermediario — numeric — NULL — default: 1.2
complexity_avancado — numeric — NULL — default: 1.5
sort_order — integer — NULL — default: 100
active — boolean — NULL — default: true
